- name: Cleanup artifacts
  run: gh api repos/${{ github.repository }}/actions/artifacts --jq '.artifacts[].id' | xargs -I{} gh api -X DELETE repos/${{ github.repository }}/actions/artifacts/{}
  continue-on-error: true
  env:
    GH_TOKEN: ${{ secrets.GITHUB_TOKEN }}